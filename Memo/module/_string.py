from string import digits, ascii_letters, ascii_lowercase, ascii_uppercase, punctuation

#print all import
print(f"{digits = !s}")
print(f"{ascii_letters = !s}")
print(f"{ascii_lowercase = !s}")
print(f"{ascii_uppercase = !s}")
print(f"{punctuation = !s}")
